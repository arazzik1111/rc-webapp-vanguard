import React from "react";
import { AvatarStack } from "../AvatarStack";
import { Story, emptyItems } from "./_AvatarStack.default";

export const Empty: Story = {
  args: {
    items: emptyItems,
  },
};