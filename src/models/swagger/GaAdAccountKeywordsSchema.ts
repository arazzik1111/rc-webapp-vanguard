import { GaAdAccountKeywordSchema } from '@models/swagger//GaAdAccountKeywordSchema';

export type GaAdAccountKeywordsSchema = {
  general?: GaAdAccountKeywordSchema[];
  negative?: GaAdAccountKeywordSchema[];
  brand?: GaAdAccountKeywordSchema[];
};
